# Resend is optional during local development. Add all three values before launch.
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=

# Optional: required for Google Ads to count phone-link clicks as a conversion.
# Use only the label portion shown after AW-17537368439/ in Google Ads.
NEXT_PUBLIC_GOOGLE_ADS_CALL_CONVERSION_LABEL=
